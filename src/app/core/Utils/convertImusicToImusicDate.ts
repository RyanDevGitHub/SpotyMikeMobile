import { ISong } from '../interfaces/song';
import { IUser } from '../interfaces/user';

// export function mapMusicToMusicDate(music: ISong, artistInfo: IUser): ISong {
//   return {
//     ...music,
//     artistInfo,
//     listeningCount: music.listeningCount.toString(),
//     createAt: new Date(
//       music.createAt.seconds * 1000 + music.createAt.nanoseconds / 1e6
//     ),
//   };
// }
